
@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head lang="en">
    <meta charset="UTF-8">
    <title>幽灵按钮</title>
    <style>
        *{
          margin:0;
          padding:0;
         }

        body{
          background-color: #333;
        }

        .box{
          width:800px;
          height: 280px;
          margin: 50px auto;
        }

        .box .link{
          width:205px;
          height: 280px;
          margin:0 20px;
          float: left;
        }

        /*transition有四个参数：
          transition-propety:设置过渡效果的CSS属性名称，对哪一部分进行transition
          transition-duration：过渡时间
          transition-timing-function：速度曲线 ease-in ease-out
          transition-delay:延时时间*/

        .link .icon{
          display: inline-block;  /*span是内联元素没有宽高*/
          width: 100%;
          height: 190px;
          transition:0.2s linear;   /*IE9及之前的版本不支持transition*/
          -webkit-transition:0.2s linear;
          -o-transition:0.2s linear;
          -moz-transition:0.2s linear;
          -ms-transition:0.2s linear;
        }

        .link-miss .icon{
          background: url("/Themes/img/mission.png") no-repeat center center;
        }

        .link-play .icon{
          background: url("/Themes/img/play.png") no-repeat center center;
        }

        .link-touch .icon{
          background: url("/Themes/img/touch.png") no-repeat center center;
        }

        /*.link:hover .icon鼠标经过盒子*/
        .link .icon:hover{
          transform:rotate(360deg) scale(1.2);
          -ms-transform:rotate(360deg) scale(1.2);
          -webkit-transform:rotate(360deg) scale(1.2);
          -o-transform:rotate(360deg) scale(1.2);
          -moz-transform:rotate(360deg) scale(1.2);
        }

        /*box-sizing匹配某个区域的特定元素*/

        .button{
          display: block;
          width: 180px;
          height: 50px;
          text-decoration: none;
          line-height: 50px;
          color:#2DCB70;
          font-family: "微软雅黑", Arial, Helvetica, sans-serif;
          font-weight: bolder;
          border: 2px solid rgba(255,255,255,0.8);
          padding-left: 20px;
          margin: 0 auto;
          box-sizing:border-box;
          -moz-box-sizing:border-box;
          -webkit-box-sizing:border-box;
        }
    </style>
</head>
<body>
    <div class="box">
        <div class="link link-miss">
            <span class="icon"></span>
            <a href="#" class="button">
                <span class="line line-top"></span>
                <span class="line line-left"></span>
                <span class="line line-right"></span>
                <span class="line line-buttom"></span>
                MISSION
            </a>
        </div>

        <div class="link link-play">
            <span class="icon"></span>
            <a href="#" class="button">
                <span class="line line-top"></span>
                <span class="line line-left"></span>
                <span class="line line-right"></span>
                <span class="line line-buttom"></span>
                PLAY
                </a>
        </div>

        <div class="link link-touch">
            <span class="icon"></span>
            <a href="#" class="button">
                <span class="line line-top"></span>
                <span class="line line-left"></span>
                <span class="line line-right"></span>
                <span class="line line-buttom"></span>
                TOUCH
            </a>
        </div>
    </div>
</body>
</html>